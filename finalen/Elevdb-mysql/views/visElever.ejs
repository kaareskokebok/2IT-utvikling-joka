<a href="/showStudents">
    <button>Vis elever</button>
  </a>
<% if(locals.elever) {%>
    <ul>
        <% elever.forEach(elev => { %>
            <li><%= elev.fornavn %>. Klasse: <%= elev.klasse %></li>
        <% }); 
        %>
    </ul>
    <% } %>